[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "project-euler"
version = "2026.6.9"
dependencies = [
    "numpy",
    "numba"
]

[project.optional-dependencies]
dev = [
    "ruff",
    "ty",
]

[tool.setuptools]
py-modules = ["funcs"]
